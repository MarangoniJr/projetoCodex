{
  "name": "reembolso-despesas",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/vps.js",
    "user:create": "node server/users.js create",
    "user:password": "node server/users.js password",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "db:generate": "drizzle-kit generate",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "vite": "^8.3.0"
  }
}
